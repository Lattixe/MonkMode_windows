<Window x:Class="MonkMode.Views.WindowPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Windows"
        Width="400" Height="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetName="MainContainer"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="MainContainer"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="6" To="0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainContainer"
            Background="#0C0C0D"
            BorderBrush="#1F1F23"
            BorderThickness="1"
            CornerRadius="12"
            Margin="16"
            Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform Y="6"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    BorderBrush="#1F1F23" 
                    BorderThickness="0,0,0,1"
                    Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Select windows"
                                   Foreground="#FAFAFA"
                                   FontSize="14"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"/>
                        <Border Background="#18181B"
                                CornerRadius="4"
                                Padding="8,3"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="TaskInfoText"
                                       Text="25m"
                                       Foreground="#52525B"
                                       FontSize="11"
                                       FontWeight="Medium"/>
                        </Border>
                    </StackPanel>
                    
                    <TextBlock x:Name="SelectionCount"
                               Grid.Column="1"
                               Text="0 selected"
                               Foreground="#3F3F46"
                               FontSize="12"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Window list -->
            <ListBox x:Name="WindowListBox"
                     Grid.Row="1"
                     Background="Transparent"
                     BorderThickness="0"
                     SelectionMode="Multiple"
                     SelectionChanged="WindowListBox_SelectionChanged"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     Padding="8,4">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder"
                                            Background="Transparent"
                                            CornerRadius="8"
                                            Padding="12,10"
                                            Margin="0,1"
                                            Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Checkbox -->
                                            <Border x:Name="Checkbox"
                                                    Grid.Column="0"
                                                    Width="18" Height="18"
                                                    CornerRadius="5"
                                                    BorderBrush="#3F3F46"
                                                    BorderThickness="1.5"
                                                    Background="Transparent"
                                                    Margin="0,0,12,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock x:Name="CheckIcon"
                                                           Text="✓"
                                                           Foreground="#FFFFFF"
                                                           FontSize="11"
                                                           FontWeight="Bold"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Margin="0,-1,0,0"
                                                           Opacity="0"/>
                                            </Border>

                                            <!-- Window info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}"
                                                           Foreground="#FAFAFA"
                                                           FontSize="13"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding ProcessName}"
                                                           Foreground="#52525B"
                                                           FontSize="11"
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <!-- Number key hint -->
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding IndexDisplay}"
                                                       Foreground="#3F3F46"
                                                       FontSize="11"
                                                       FontFamily="Consolas"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0,0,0"/>
                                        </Grid>
                                    </Border>
                                    
                                    <ControlTemplate.Triggers>
                                        <!-- Hover -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#111114"/>
                                            <Setter TargetName="Checkbox" Property="BorderBrush" Value="#52525B"/>
                                        </Trigger>
                                        
                                        <!-- Selected -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#111827"/>
                                            <Setter TargetName="Checkbox" Property="Background" Value="#3B82F6"/>
                                            <Setter TargetName="Checkbox" Property="BorderBrush" Value="#3B82F6"/>
                                            <Setter TargetName="CheckIcon" Property="Opacity" Value="1"/>
                                        </Trigger>
                                        
                                        <!-- Selected + Hover -->
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#1E3A5F"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Footer -->
            <Border Grid.Row="2" 
                    BorderBrush="#1F1F23" 
                    BorderThickness="0,1,0,0"
                    Padding="12,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Hint -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#18181B"
                                CornerRadius="4"
                                Padding="6,3"
                                Margin="0,0,6,0">
                            <TextBlock Text="1-9"
                                       Foreground="#52525B"
                                       FontSize="10"
                                       FontFamily="Consolas"/>
                        </Border>
                        <TextBlock Text="quick select"
                                   Foreground="#3F3F46"
                                   FontSize="11"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="CancelButton"
                                Margin="0,0,8,0"
                                Cursor="Hand"
                                Click="Cancel_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="Transparent"
                                            CornerRadius="6"
                                            Padding="12,8">
                                        <TextBlock Text="Cancel"
                                                   Foreground="#71717A"
                                                   FontSize="12"
                                                   FontWeight="Medium"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#18181B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button x:Name="StartButton"
                                Content="Start"
                                IsEnabled="False"
                                Cursor="Hand"
                                Click="Start_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="#FAFAFA"
                                            CornerRadius="6"
                                            Padding="16,8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          TextBlock.Foreground="#09090B"
                                                          TextBlock.FontSize="12"
                                                          TextBlock.FontWeight="Medium"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#E4E4E7"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Background" Value="#27272A"/>
                                            <Setter Property="Foreground" Value="#52525B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
